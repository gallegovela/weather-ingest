AEMET_API_KEY=

# PostgreSQL connection string used by Alembic (db/) and by the app.
# Format: postgresql+psycopg://user:password@host:5432/db
DATABASE_URL=

# Minutes a control panel session stays valid from login, without
# renewal (defaults to 15 if not set).
CONTROL_SESSION_TTL_MINUTES=
